ModelExecutor Left = new ModelExecutor(Model model)
ModelExecutor Right = new ModelExecutor(Model model)
ModelMerge Merge = new MoodelMerge(left, right)




Add tabs to Column Compare to show Only rows and columns where there are mismatches?
Add record nav to source, target, compare, Erroes
Allow for Numeric Filtering In(1,2,3)
Fix grid doesn't save on lost focus
Hook up Refresh
Add left, right field to datasource so we don't have to figure it out
